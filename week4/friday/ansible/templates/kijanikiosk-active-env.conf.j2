location / {
    proxy_pass         http://kk-api-blue;
    proxy_http_version 1.1;
    proxy_set_header   Host $host;
    proxy_cache_bypass $http_upgrade;
}

location /health {
    proxy_pass http://kk-api-blue;
}